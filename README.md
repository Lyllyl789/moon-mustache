# moon-mustache

A specification-compliant Mustache template engine in pure MoonBit.

## Features

- Spec-compliant interpolation, sections, inverted sections, comments, partials, and delimiter changes.
- Pure MoonBit, zero dependencies (except `moonbitlang/core`).
- Targets Wasm, JS, and Native.
